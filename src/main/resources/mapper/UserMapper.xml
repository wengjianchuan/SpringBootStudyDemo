<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingting.mapper.UserMapper">

	<resultMap id="BaseResultMap" type="com.lingting.model.User">
		<id column="user_id" property="userId" jdbcType="INTEGER"/>
		<id column="user_name" property="userName" jdbcType="VARCHAR"/>
		<id column="user_password" property="userPwd" jdbcType="VARCHAR"/>
	</resultMap>

	<!-- <select id="selectByKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
		select user_id, user_name, user_password from t_user where user_id = #{userId, jdbcType=INTEGER}
	</select> -->
</mapper>